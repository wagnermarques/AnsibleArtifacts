---
# defaults file for container-basic
default_build_directory : "{{ playbook_dir }}/{{ default_container_name }}"
default_container_image : alpine
default_container_name : alpine_container
default_container_command : ls -lha
numbers_of_container_instances : 2
