---
- name: Install AI CLI tools via NPM
  npm:
    name: "{{ item }}"
    global: yes
    state: present
  loop: "{{ ai_npm_packages }}"

- name: Check for Python AI tools
  pip:
    name: "{{ ai_python_packages }}"
    state: present
    executable: pip3
    extra_args: "--break-system-packages"
